// usersFavoritedPictures = picturesDisplayArr.filter((picture) => {
    //     //console.log(typeof(userToken.favorites[0]));
    //     usersFavoritesIds.includes(picture.id)});

    // let usersFavoritedPictures = picturesDisplayArr.filter((pic) => {
    //     picturesDisplayArr.filter(person => id_filter.includes(person.id))
    //     // let currTokenSearch = userToken.favorites.indexOf(pic.id);
    //     // if(currTokenSearch != -1){
    //     //     return true;
    //     // }
    //     // else{
    //     //     return false;
    //     // }
    //     //pic.id == userToken
    //     //userToken.favorites.indexOf(pic.id) >= 0;
    //     // console.log("type of pic.id" + typeof(pic.id));
    //     // console.log("type of pic.id" + typeof(pic.id + ""));
    //     // console.log("type of usertok" + typeof(userToken.favorites[0]));
    //     // console.log(userToken.favorites);
        
    //     // for (let userFavIdx = 0; userFavIdx < userToken.favorites.length; userFavIdx++){
    //     //     //console.log("pic.id type" + typeof(pic.id) + " pic.id val" + pic.id);
    //     //     //console.log("userToken.favorites[userFavIdx] type" + typeof(userToken.favorites[userFavIdx]) + " userToken.favorites[userFavIdx] val" + userToken.favorites[userFavIdx]);
    //     //     return (pic.id == +(userToken.favorites[userFavIdx]));
                
            
    //     // }
    // });